# Copy to .env (gitignored) to override the defaults docker-compose.yml falls
# back to. Not required to run the stack locally — only needed if you want
# credentials different from the ragplatform/ragplatform default.
DB_NAME=ragplatform
DB_USER=ragplatform
DB_PASSWORD=ragplatform
